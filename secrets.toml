# .streamlit/secrets.toml  —  do NOT commit to Git (.gitignore it)

[auth]
jwt_secret  = "4239c79119dba6a3d0e6cfd1dccae74c63f3fa99d77e838947925e49f7331136"
token_hours = 12

[email]
smtp_server  = "smtp.gmail.com"
smtp_port    = 587
sender       = "dominate3ai@gmail.com"
# PASTE YOUR 16-CHAR GMAIL APP PASSWORD HERE (no spaces). Generate it at
# https://myaccount.google.com/apppasswords with 2-Step Verification ON.
# The app now strips spaces automatically, but keep it clean anyway.
app_password = "YOUR_16_CHAR_APP_PASSWORD"
# security   = "starttls"   # optional: "starttls" | "ssl" | "none" (auto from port)
# timeout    = 30           # optional: seconds

# Optional — better-quality company logos (any of 50M+ companies) on the report
# cover page and in the org dropdown. Leave this section out entirely and the app
# falls back to a free, key-free favicon service. Get a publishable token (free
# tier) at https://www.logo.dev
[company]
logo_dev_token = "YOUR_LOGO_DEV_PUBLISHABLE_TOKEN"
